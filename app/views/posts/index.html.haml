- if user_signed_in?
  %p#intro
    Hello
    = current_user.name
    %br/
    %span
      Save Notes on Startups - Austin Version
- else
  %p#intro
    Startup Pinboard - Sign In to Start
    %br/
    %span
      Thoughts & Notes on Startups

#posts
  - @posts.each do |post|
    .post
      .post_image
        = link_to (image_tag post.image.url(:small)), post
      .post_content.bannertag
        .title
          %h2= link_to post.title, post
          %h3= link_to "Visit Link", "http://" + post.link, :target => "_blank", class: "pull-right data"
        .data.clearfix
          %p.username
            Shared by
            = post.user.name
          %p.buttons
            %span
              %i.fa.fa-comments-o
              =post.comments.count
            %span
              %i.fa.fa-comments.-o
              =post.get_likes.size
