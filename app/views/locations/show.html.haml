%h1= @location.location_name

= link_to "Edit", edit_location_path(@location), class: "data"
= link_to "Delete", location_path(@location), method: :delete, data: { confirm: "Are you sure?" }, class: "data"
= link_to "New", new_location_path, class: "button"
= link_to "Home", root_path, class: "button"
